husisha C::stdio
husisha { ../../msingi/mkusanyaji/msambazaji.swa }

n32 main(n32 argc, n8** argv) {
    andika("; A\n");
    n32 name_node = node_mpya(AST_KITAMBULISHO, 0, -1, -1);
    andika("; B: idx=%d\n", name_node);
    // Test if the crash is in the string literal passing or hifadhi_jina itself
    // Just print the string to test string literal passing
    andika("; string test: %s\n", "jaribu");
    andika("; C\n");
    rudisha 0;
}
