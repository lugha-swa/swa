# Jina la utendaji
name: Majaribio

# Wakati wa kuendesha
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Kazi za kufanya
jobs:
  jaribu:
    name: Jenga na Jaribu
    runs-on: ubuntu-latest

    steps:
      - name: Pakua msimbo
        uses: actions/checkout@v4

      - name: Weka Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Weka LLVM
        run: |
          sudo apt-get update
          sudo apt-get install -y llvm-22-dev libclang-22-dev clang-22
          echo "LLVM_SYS_220_PREFIX=/usr/lib/llvm-22" >> $GITHUB_ENV

      - name: Jenga
        run: cargo build --release

      - name: Jaribu
        run: cargo test
